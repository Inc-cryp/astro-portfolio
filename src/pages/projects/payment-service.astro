---
import BaseLayout from "@/layouts/BaseLayout.astro";
import Section from "@/components/Section.astro";

const project = {
    title: "Payment Processing Service",
    summary:
        "Backend service for handling secure and reliable payment transactions.",
};
---

<BaseLayout>
    <!-- HEADER -->
    <Section>
        <h1 class="text-3xl font-bold mb-2">{project.title}</h1>
        <p class="text-gray-600 dark:text-gray-400 max-w-xl">
            {project.summary}
        </p>
    </Section>

    <!-- SYSTEM OVERVIEW -->
    <Section title="System Overview">
        <pre>
Client
  │
  ▼
API Gateway
  │
  ▼
Payment Service ─────▶ Redis (Idempotency Key)
  │
  ▼
PostgreSQL ─────────▶ Payment Provider API
    </pre>
    </Section>

    <!-- PROBLEM -->
    <Section title="Problem">
        <p>
            Handling duplicate payment requests and ensuring data consistency
            during network retries was a major challenge.
        </p>
    </Section>

    <!-- SOLUTION -->
    <Section title="Solution">
        <ul class="list-disc ml-5 space-y-2">
            <li>Idempotency keys to prevent duplicate charges</li>
            <li>Transaction-safe databas</li>
        </ul></Section
    ></BaseLayout
>
